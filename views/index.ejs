<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>A New Startup: Sign Up Today!</title>
  </head>

  <body>
      <h2 class="text-muted">Tours Shared</h2>
      <div>
        <ul>
          <li><a href="/add-request">Add request</a></li>
        </ul>
        <% requests?.forEach(function(request) { %>
          <div>
            <b><%= request.title.S %></b>
            <div><%= request.description.S %></div>
          </div>
        <%})%>
      </div>
  </body>
</html>
